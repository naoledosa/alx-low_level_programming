#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: number being tested
 * @b: number being tested
 * Return: always 0.
 */

int mul(int a, int b)
{
	
	return (a * b);
}
