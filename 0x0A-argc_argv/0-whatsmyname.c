#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints its name, followed by a new line.
 * Return: its own name.
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
