#include "main.h"

/**
 * main - tests function that prints if integer is positive or negative
 * Return: 0
 */

int main(void)
{
	int number;

	number = 98;
	postitive_or_negative(number);

	return (0);
}
