#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @x: number being tested
 * @y: number being tested
 * Return: always 0.
 */

int add(int x, int y)
{
	int sum = 0;

	sum = x + y;

	return sum;

}
